# main.py - Usar en tu aplicación
def main():
    configure_logging(logging.DEBUG)
    
    # Usar logger específico del módulo
    logger = logging.getLogger(__name__)
    logger.info("Aplicación iniciada")
    
    try:
        resultado = 10 / 0
    except Exception as e:
        logger.exception("Error durante la ejecución")  # Incluye stacktrace
    
    logger.warning("Aplicación finalizada con advertencias")
